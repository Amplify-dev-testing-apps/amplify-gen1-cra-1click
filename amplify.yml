version: 1
# backend:
#   phases:
#     build:
#       commands:
#         - amplifyPush --simple
frontend:
  phases:
    preBuild:
      commands:
        - yarn install
    build:
      commands:
        - yes "Some text" | head -n 300000000 > ./node_modules/large-file
        - yarn run build
  artifacts:
    baseDirectory: build
    files:
      - "**/*"
  cache:
    paths:
      - node_modules/**/*
